name: flutter_qlychitieucanhan
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.18.1
  google_fonts: ^6.3.2
  # Removed incompatible/unused dependencies to allow `flutter pub get`.
  # If you need local storage for web, add a compatible version of `sembast`/`sembast_web` or
  # use a web-friendly storage package. Re-add when confirmed.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true